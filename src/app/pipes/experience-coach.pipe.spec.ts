import { ExperienceCoachPipe } from './experience-coach.pipe';

describe('ExperienceCoachPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceCoachPipe();
    expect(pipe).toBeTruthy();
  });
});
