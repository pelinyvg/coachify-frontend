import { TopicNameFilterPipe } from './topic-name-filter.pipe';

describe('TopicNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
